ripgrep make mingw fd unzip gzip wget git


when using godot
if neovide
--server 127.0.0.1:6004 --remote-send "<esc>:n {file}<CR>:call cursor({line},{col})<CR>"

if vscode

{project} --goto {file}:{line}:{col} 